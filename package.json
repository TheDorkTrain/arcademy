{
  "name": "gamehub",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "concurrently \"cd backend/login && python app.py\" \"cd backend && python -m would_you_rather_api.questions_flask_api\" \"cd frontend && npm start\"",
    "install": "concurrently \"cd backend && pip install -r requirements.txt\" \"cd frontend && npm install\"",
    "build": "cd frontend && npm run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "bootstrap": "^5.3.3",
    "express": "^4.19.2",
    "react-bootstrap": "^2.10.2"
  }
}
